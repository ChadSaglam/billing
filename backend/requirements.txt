# Security-sensitive packages use >= so a patched release is picked up.
# Everything else is pinned. After changing this file run:
#   pip install -r requirements-dev.txt && pip-audit -r requirements.txt

# ── Web ───────────────────────────────────────────────────
# fastapi is unpinned upward so pip resolves a starlette without the
# 7 advisories open against 0.38.6 (R-39).
fastapi>=0.118.0
uvicorn==0.30.6

# ── Database ──────────────────────────────────────────────
sqlalchemy==2.0.35
alembic==1.13.2
psycopg2-binary==2.9.11

# ── Settings / validation ─────────────────────────────────
# Left open so pip can satisfy whatever the resolved fastapi requires.
pydantic-settings>=2.5.2
pydantic[email]>=2.9.2

# ── Auth ──────────────────────────────────────────────────
# 3.3.0 had PYSEC-2024-233 and PYSEC-2025-185. This library signs and
# verifies every JWT in the app.
python-jose[cryptography]>=3.4.0
bcrypt==4.0.1

# ── Uploads ───────────────────────────────────────────────
# Handles the logo upload; 0.0.9 had 7 advisories.
python-multipart>=0.0.20

# ── PDF / Swiss QR ────────────────────────────────────────
# R-18: reportlab + qrcode are the pair pdf_generator.py actually imports.
# weasyprint (plus its pango/cairo system packages), qrbill and svgwrite
# were installed but never imported anywhere — dropped.
reportlab==4.1.0
qrcode[pil]>=7.0
Pillow>=10.0
python-stdnum==2.2

# ── Misc ──────────────────────────────────────────────────
jinja2==3.1.6
httpx==0.27.2
slowapi==0.1.9
# The HTML sanitiser for anything rendered into a PDF or email.
bleach>=6.4.0
python-dateutil==2.9.0
